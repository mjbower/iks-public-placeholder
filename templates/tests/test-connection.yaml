apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iks-tp-stack.fullname" . }}-test-connection"
  labels:
    {{- include "iks-tp-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iks-tp-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
